# Storybook

Writing/editing/debugging stories:

- Invoke `storybook` skill first
- Use `storybook-specialist` subagent to write stories
- Many stories → launch multiple storybook-specialist agents in parallel
