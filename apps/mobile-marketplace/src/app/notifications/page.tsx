// RSC entry point — no auth/data fetch yet. Shell renders placeholder data.
import NotificationsShell from "./shell";

export default function NotificationsPage() {
	return <NotificationsShell />;
}
