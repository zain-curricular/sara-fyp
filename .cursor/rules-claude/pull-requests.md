# Pull Requests

Before writing PR:

1. **Review all commits** on branch ahead of merge target (`git log` + `git diff <base>...HEAD`)
2. **Write detailed summary** — what changed and why, not file lists
3. **Include manual testing checklist** — verification steps as markdown checklist
