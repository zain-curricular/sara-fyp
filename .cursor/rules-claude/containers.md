# Containers

- Every container need structural purpose. No pointless wrappers
- `gap-*` for spacing (not `space-*` or child margins)
- Tailwind only for styling
- Tag wrappers with `container-id`
- shadcn/ui for primitives, Tailwind for layout
