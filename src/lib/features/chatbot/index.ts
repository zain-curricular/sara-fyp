// ============================================================================
// Chatbot — Client Barrel
// ============================================================================
//
// Client-safe exports for the chatbot feature. Only types.
// Services are server-only and accessed via the /api/chatbot route.

export type { ChatMessage, ChatSession, Citation } from "./types";
