// ============================================================================
// Admin Feature — Client Barrel
// ============================================================================
//
// Export types and client-safe utilities from the admin feature.
// Server-only services live in services.ts — never import them here.

export type {
	AdminAction,
	AdminCategory,
	AdminDispute,
	AdminKPIs,
	AdminListing,
	AdminMechanic,
	AdminOrder,
	AdminPayout,
	AdminSeller,
	AdminUser,
	AdminVehicle,
	FraudSignal,
	KBDocument,
	PlatformSetting,
} from "./types";
